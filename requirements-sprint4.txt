# Optional dependencies for Sprint 4 production-style runs.
# Core Sprint 2 remains installable with requirements.txt alone.

openenv-core>=0.2.3
trl>=0.9.0

# Optional low-VRAM training stack (Linux + CUDA focused):
# unsloth
# vllm

